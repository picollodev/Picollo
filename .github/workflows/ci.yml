name: CI

on:
  push:
    branches: [ main ]
    tags:
      - 'v*'
  pull_request:
    branches: [ main ]

permissions:
  id-token: write # Mandatory for Trusted Publishing (OIDC)
  contents: read

jobs:
  build-test-publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Build
        run: dotnet build Picollo.slnx -c Release

      - name: Test
        run: dotnet test Picollo.slnx -c Release --no-build

      - name: Set package version
        if: startsWith(github.ref, 'refs/tags/v')
        run: echo "PACKAGE_VERSION=${GITHUB_REF_NAME#v}" >> $GITHUB_ENV

      - name: Pack
        if: startsWith(github.ref, 'refs/tags/v')
        run: dotnet pack Picollo.slnx -c Release --no-build -o ./artifacts -p:PackageVersion=$PACKAGE_VERSION

      - name: Publish to NuGet
        if: startsWith(github.ref, 'refs/tags/v') && github.repository == 'picollodev/picollo'
        run: |
          dotnet nuget push "./artifacts/*.nupkg" --source https://api.nuget.org/v3/index.json --skip-duplicate --api-key ${{ secrets.NUGET_API_KEY }}
